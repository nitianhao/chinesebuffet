/**
 * Reusable layout and typography class names (Tailwind only).
 * Use these for consistent spacing and typography across pages.
 * No extra dependencies.
 */

/** Page content container: centered, max width, responsive horizontal padding */
export const pageContainer =
  'mx-auto max-w-6xl px-4 sm:px-6 lg:px-8';

/** Vertical spacing for major sections */
export const sectionY =
  'py-10 sm:py-12';

/** Section heading (h2) style */
export const h2 =
  'text-lg font-semibold tracking-tight';

/** Muted/secondary text (uses design system var if present) */
export const muted =
  'text-sm text-muted-foreground';
