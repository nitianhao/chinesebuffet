/**
 * Simple text skeleton placeholder for Suspense fallbacks.
 * Minimal UI - no new design features.
 */
export default function SectionFallback() {
  return (
    <div className="h-12 rounded bg-[var(--muted)]/20 animate-pulse" aria-hidden />
  );
}
